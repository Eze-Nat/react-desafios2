


const ItemListTest = ({items, test}) => {
    const {nombre, descripcion, precio} = items;
    return (  
        <div>
            <h1>{precio}</h1>
        </div>
    );
}
 
export default ItemListTest;